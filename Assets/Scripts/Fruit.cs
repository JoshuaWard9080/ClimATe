using UnityEngine;
using UnityEngine.UI;


public class Fruit : MonoBehaviour
{
    [SerializeField] public Sprite fruitImg1;
    [SerializeField] public Sprite fruitImg2;
    [SerializeField] public Sprite fruitImg3;
    [SerializeField] public Sprite fruitImg4;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
